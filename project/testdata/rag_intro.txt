检索增强生成（Retrieval-Augmented Generation，RAG）是一种将外部知识库与大语言模型结合的技术。
其核心流程通常包含四个步骤：文档解析与切片、向量化入库、相似度检索、以及基于检索结果的生成。

在学术问答场景中，RAG 的主要优势是可溯源。模型回答应严格依据检索到的文献片段，并标注引用编号。
若检索结果不足以支撑结论，系统应明确告知“根据现有资料无法回答该问题”，而不是编造内容。

切片策略对召回质量影响很大。过短的切片会丢失上下文，过长的切片会稀释语义。
常见做法是按段落优先切分，并设置 10% 左右的重叠，以降低边界信息丢失。
