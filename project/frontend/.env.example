# 前端环境变量示例（复制为 frontend/.env.local 后按需修改）

# 后端 API 地址。
# 留空时走 vite.config.ts 中的 /api 代理（开发环境推荐），
# 前后端分离部署到不同域名时才需要填写，例如：
# VITE_API_BASE_URL=https://api.your-domain.com
VITE_API_BASE_URL=

# 仅开发环境生效：Vite 代理的后端目标地址
# VITE_API_TARGET=http://127.0.0.1:8000
